# Program pro výpočet součtu čísel od 1 do 100

# Výpočet pomocí vestavěné funkce sum() a rozsahu range(1, 101)
# range(1, 101) generuje čísla od 1 do 100 (horní meze se neúčastní)
soucet = sum(range(1, 101))

# Výpis výsledku do konzole
print(f"Součet čísel od 1 do 100 je: {soucet}")